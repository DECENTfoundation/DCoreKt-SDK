language: java
install: true
sudo: required
services: docker

env:
  - DOCKER_COMPOSE_VERSION=1.23.2

before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - curl -Ls -o codacy-coverage-reporter-assembly.jar $(curl -Ls https://api.github.com/repos/codacy/codacy-coverage-reporter/releases/latest | jq -r '.assets | map({content_type, browser_download_url} | select(.content_type | contains("java-archive"))) | .[0].browser_download_url')

jdk:
  - oraclejdk9

before_script:
#  - ls -al
#  - sudo chown -R 1000 test-dcore-node
#  - ls -al
#  - cd ./test-dcore-node
#  - ls -al
#  - cd ./datadir
#  - ls -al
#  - id travis

#  - docker-compose up -d

#  - docker-compose ps
#  - docker-compose exec DCore ls -al
#  - docker-compose exec DCore bash -c "cd ./.decent"
#  - docker-compose exec DCore ls -al
#  - docker-compose exec DCore id dcore

script:
  - ./gradlew build
  - java -jar codacy-coverage-reporter-assembly.jar report -l Java -r library/build/reports/jacoco/jacocoTestReport.xml
deploy:
  - provider: pages
    skip_cleanup: true
    local_dir: library/build/javadoc/
    github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
    keep_history: true
    on:
      tags: true
before_deploy:
  - touch library/build/javadoc/.nojekyll
