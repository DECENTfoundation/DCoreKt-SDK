language: java
install: true
sudo: required
services: docker

jdk:
  - oraclejdk11

before_script:
  - docker-compose run --user='root' --entrypoint chown -R myuser:myuser .

script:
  - ./gradlew test
