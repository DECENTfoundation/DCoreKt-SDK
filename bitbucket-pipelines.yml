image: java:8

pipelines:
  branches:
    master:
      - step:
          script:
            - ./gradlew build --stacktrace
            - ./gradlew artifactoryPublish -DARTIFACTORY_REPO=gradle-release-local
    develop:
      - step:
          script:
            - ./gradlew build --stacktrace
            - ./gradlew artifactoryPublish -DARTIFACTORY_REPO=gradle-dev-local
