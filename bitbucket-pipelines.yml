image: java:8

pipelines:
  default:
      - step:
          caches:
            - gradle
          script:
            - ./gradlew check build --stacktrace
            - ./gradlew artifactoryPublish
